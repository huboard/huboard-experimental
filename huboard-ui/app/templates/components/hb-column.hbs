<h3> {{column.name}} </h3>
<ul class="cards">
  {{#each issues as |issue|}}
    {{hb-card issue=issue}}
  {{/each}}
</ul>

<ul class="not-cards">
  {{#each issues as |issue|}}
    <li> {{issue.title}} </li>
  {{/each}}
</ul>
